info:
  name: local market
  type: http
  seq: 1

http:
  method: POST
  url: POST http://localhost:1337/api/products
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
